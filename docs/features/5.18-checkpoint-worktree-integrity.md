# 5.18. Checkpoint & Worktree Integrity Guard

**Status:** Proposed  
**Owner docs:** `docs/report/git_parse_debug_report.md` — Gap G  
**Code areas:** `server/internal/orchestrator/patch/applier.go`, `server/internal/orchestrator/steps/` (code_backend, code_frontend, fix), Orchestrator Workflow Engine  
**Acceptance criteria:**
- Mỗi step thành công tạo một Git commit checkpoint trong worktree.
- Resume/rollback task khôi phục worktree về đúng commit checkpoint tương ứng và re-index cache.
- Retry loop reset worktree về HEAD trước khi thử lại.
- Revert failures được log rõ ràng, không bị bỏ qua âm thầm.

**Mục tiêu:** Đảm bảo tính toàn vẹn (integrity) của worktree trong suốt vòng đời thực thi của task — cả ở mức macro (giữa các step trong workflow) lẫn mức micro (giữa các lần retry trong một step). Sử dụng Git commits làm Single Source of Truth để snapshot trạng thái file, kết hợp với stateless cache re-indexing (Option A).

---

## A. Vấn Đề Hiện Tại

Hệ thống hiện tại gặp các lỗi nghiêm trọng ở hai cấp độ:

### 1. Micro-level: Silent Revert Failures
Khi patch thất bại, `applier.go:307` cố gắng revert nhưng **bỏ qua lỗi âm thầm** (`_, _ = r.RunSandboxStepInWorktree(...)`). Chuỗi apply (`git apply` → `patch --batch`) cho phép `patch` apply từng hunk riêng lẻ — nếu một số hunk thành công và một số thất bại, file sẽ bị **truncated/corrupted**.

**Bằng chứng:** Trong workspace `tool_zentao`, call-002 dùng module `zentao.com/gitlab_sync`. Revert thất bại âm thầm, file bị giữ lại. Call-003 cố tạo `new file` trên file đã tồn tại, `patch` ghi đè một phần → file bị truncated, mất closing `}`.

### 2. Macro-level: Không có File Snapshot tại Checkpoint
Orchestrator lưu workflow state (JSON) vào database tại mỗi checkpoint, nhưng **không snapshot file vật lý** hay SQLite cache. Khi user resume từ step cũ, source code và cache vẫn ở trạng thái mới nhất — hoàn toàn lệch pha với workflow state.

---

## B. Giải Pháp: Git Commits + Re-indexing (Option A)

### 1. Macro-level: Step Checkpoints

Khi một step hoàn thành thành công, orchestrator tự động tạo Git commit:

```bash
git add .
git commit -m "chore(auto-code-os): checkpoint [step_id]"
```

Khi resume hoặc rollback về một checkpoint cũ:

```text
   [Resume từ Checkpoint X]
            │
            ▼
   [git checkout <commit_hash>]
            │
            ▼
   [git reset --hard && git clean -fd]
            │
            ▼
   [Xóa workspace_cache.db]
            │
            ▼
   [Gọi IndexWorkspace() → Re-build AST Cache]
            │
            ▼
   [Tiếp tục từ Step X+1 với dữ liệu sạch]
```

**Tại sao không backup file `.db`?** Vì cache SQLite là **stateless** — nó được sinh hoàn toàn từ source code. Xóa và re-index đảm bảo Code ↔ Cache luôn đồng bộ 100%, không lo lệch pha.

### 2. Micro-level: Pre-Retry Worktree Reset

Trong vòng lặp retry của `code_backend.go`, `code_frontend.go`, `fix.go`:

```go
// Trước mỗi lần retry (attempt >= 2):
if attempt > 1 {
    resetCmd := "git reset --hard HEAD && git clean -fd"
    _, err := s.sandbox.RunInWorktree(ctx, task, agent, stepID+"_retry_reset", resetCmd, worktreeSuffix)
    if err != nil {
        s.log.Log(ctx, task.ID, &jobID, "error",
            fmt.Sprintf("Cannot reset worktree before retry: %v. Aborting step.", err))
        return nil, fmt.Errorf("worktree corrupted, cannot retry: %w", err)
    }
}
```

**Tại sao không cần re-index DB ở đây?** Vì DB chỉ được cập nhật khi step thành công. Các attempt thất bại không ghi gì vào cache.

### 3. Failsafe: Log Revert Outcomes

**File:** `applier.go` (Lines 307 VÀ 398 — cả single-repo và multi-repo)

```go
// BEFORE:
_, _ = r.RunSandboxStepInWorktree(ctx, task, agent, stepID+"_revert_patch", revertCmd, worktreeSuffix)

// AFTER:
_, revertErr := r.RunSandboxStepInWorktree(ctx, task, agent, stepID+"_revert_patch", revertCmd, worktreeSuffix)
if revertErr != nil && r.Log != nil {
    r.Log(ctx, task.ID, "error", fmt.Sprintf("REVERT FAILED for %s: %v", stepID, revertErr))
}
```

---

## C. Affected Files

| File | Thay đổi |
|------|----------|
| `server/internal/orchestrator/worker.go` (hoặc Engine) | Thêm logic Git commit khi save checkpoint; Git checkout + cache re-index khi resume |
| `server/internal/orchestrator/steps/code_backend.go` | Thêm `git reset --hard HEAD && git clean -fd` trước retry |
| `server/internal/orchestrator/steps/code_frontend.go` | Tương tự |
| `server/internal/orchestrator/steps/fix.go` | Tương tự |
| `server/internal/orchestrator/patch/applier.go` | Log revert errors (lines 307 và 398) |

---

## D. Acceptance Criteria

- [ ] Mỗi step thành công tạo ra một Git commit trong worktree
- [ ] Resume task từ checkpoint cũ checkout đúng commit tương ứng
- [ ] Resume task xóa `workspace_cache.db` cũ và trigger `IndexWorkspace()` mới
- [ ] Trong retry loop, `git reset --hard HEAD && git clean -fd` chạy trước attempt 2, 3, ...
- [ ] Revert errors trong `applier.go` được log với level `"error"` thay vì bị discard
- [ ] Test: simulate partial patch apply + failed revert → verify hard-reset khôi phục clean state
