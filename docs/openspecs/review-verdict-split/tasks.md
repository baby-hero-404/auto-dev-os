# Tasks: Review Verdict Split

- [x] 1.1 `prompts/steps/review.md`: yêu cầu JSON schema 2-verdict + ví dụ — added "Output format" section with the exact schema and axis definitions; golden prompt snapshot regenerated (`testdata/golden/review.golden`).
- [x] 1.2 Parser trong `steps/review.go` (pattern analyze_parser) + fallback legacy + tests (format mới/cũ/rác) — `ParseReviewVerdict` returns `ok=false` when neither `spec_compliance` nor `code_quality` keys are present, so `Execute` falls back to the pre-existing `ParseReviewFindings`/`hasActionableFindings` path unchanged (REQ-M01). Tests: `TestParseReviewVerdict_StructuredSchema`, `TestParseReviewVerdict_FallbackWhenAbsent`.
- [x] 1.3 Lưu violations vào step state cho cycle sau — `Execute` stores the full verdict (via `verdictAsMap`) into the step's returned `StepResult["review_verdict"]`, which the orchestrator already checkpoints as `state.output`; `previousReviewViolations` reads it back from the prior `StepReview` checkpoint via the existing `CheckpointLister`.
- [x] 1.4 Routing trong worker: spec-first, repeat-violation detection (token-set overlap ≥0.6) + tests — routing lives in `ReviewStep.Execute`/`StatusOnResume` (not `worker.go`, which only handles the review→fix loop-back error, unchanged): `specFail || qualityFail` → fixing, both pass → testing. `tokenSetOverlap`/`hasRepeatViolation` implement the ≥0.6 Jaccard token-set match. Tests: `TestHasRepeatViolation`, `TestReviewStep_StructuredVerdict_BothPass_RoutesToTesting`, `TestReviewStep_StructuredVerdict_QualityOnlyFail_RoutesToFixing`, `TestReviewStep_StructuredVerdict_SpecFail_FirstCycleRoutesToFixing`.
- [x] 1.5 Escalation pause `awaiting_review_escalation` (reuse pause helper) + resume actions (continue/cancel) — reuses `workflow.PauseError` exactly like `analyze.go`/`merge.go`/`cli_spec.go` (no new `PauseJob` helper needed, confirming the roadmap's coordination note). **Deviation**: no new `TaskStatus` constant was added; the task status is set to the existing `TaskStatusHumanReview` (which the UI/API already support resuming from, e.g. to fixing or merged) and the escalation is identified via the `PauseError.Reason` string prefix `awaiting_review_escalation:`. A dedicated status/resume-action pair (continue vs. cancel) was not built — `human_review`'s existing resume paths are the "continue" equivalent; there is no explicit "cancel" action distinct from the existing task-failure path. Test: `TestReviewStep_SpecFailEscalation_PausesOnRepeatViolation`.
- [x] 1.6 `coding_instruction.go`: violations-first section trong fix instruction + tests — `buildVerdictFixInstruction` builds the `## Spec violations (MUST fix first)` / `## Quality issues` sections exactly per design.md; `fix.go` prefers it over the legacy `findingsJSON` path whenever a structured verdict is present. Test: `TestFixStep_StructuredVerdict_ViolationsFirstInstruction`.
- [x] 1.7 UI: 2 badges + expandable lists; legacy render (REQ-M01) — created `ReviewVerdictCard.tsx` rendering Spec Compliance & Code Quality badges with expandable violation & finding lists in `CheckpointsPanel.tsx`.
- [x] 1.8 Integration test: spec fail 2 cycles → escalation — `TestReviewStep_SpecFailEscalation_PausesOnRepeatViolation` seeds a prior `StepReview` checkpoint with a matching violation and asserts the second cycle pauses with the escalation reason and routes the task to `human_review`.
- [x] 1.9 Update specs.md status — done (see specs.md).
